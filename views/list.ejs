<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <div class="box" id="heading">
        <% if(kindOfday==="Saturday"||kindOfday==="Sunday"){ %>
            <h1 style="color:purple"><%= kindOfday %></h1>
        <% } else { %>
            <h1 style="color:blue"><%= kindOfday %></h1>
        <% } %>
    </div>

    <div class="box">
        <% for(i=0;i<newListItems.length;i++){ %>
            <div class="item"> <input type="checkbox" name="" id=""> <p><%= newListItems[i] %></p></div>
           <% } %>

        <form action="/" method="post" class="item">
        <input type="text" name="newItem" placeholder="Add new item" autocomplete="off">
        <button type="submit">+</button>
        </form>
    </div>
    
</body>
</html>